spring:
  datasource:
    url: jdbc:mysql://localhost:3306/springboot_socks?useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver

mybatis:
  configuration:
   # 开启驼峰映射
   map-underscore-to-camel-case: true

logging:
  level:
    #打印SQL信息
    com.hehe.mapper: debug
